const $like = document.querySelector("#like");
$like.addEventListener("click", () => ($like.style.color = "red"));
document.addEventListener('DOMContentloaded', (Event) => {
 console.log('DOM fully loadede and parsed');
});